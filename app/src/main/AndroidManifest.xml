<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.up.pizzadelivery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pizzadelivery"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pizzadelivery_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".view.EscolherTamanhoActivity"></activity>
        <activity android:name=".View.PagamentoPedidoActivity" />
        <activity android:name=".View.CardapioActivity" />
        <activity android:name=".View.CadastroDeUsuariosActivity" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>