<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.up.pizzadelivery">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pizzadelivery"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pizzadelivery_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".view.DetalhesPedidoActivity"></activity>
        <activity android:name=".view.CardapioInicialActivity" />
        <activity android:name=".view.AreaClienteActivity" />
        <activity
            android:name=".view.BordaActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.CarrinhoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.BebidasActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.RelatorioPedidoActivity" />
        <activity android:name=".view.PerfilUsuarioActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".view.EscolherTamanhoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.PagamentoPedidoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.CardapioActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.CadastroDeUsuariosActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>